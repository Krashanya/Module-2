# Lambda Function in Python: Multiplication of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their product.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a*b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
f= lambda a,b: a*b

a=int(input())

b=int(input())

print(f(a,b))

## Output
<img width="324" height="231" alt="image" src="https://github.com/user-attachments/assets/4bd48f64-8f52-43f5-aacd-1c6b845b4b6c" />

## Result
Thus a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their product is created.
